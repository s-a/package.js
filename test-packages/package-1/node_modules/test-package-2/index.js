var Package = function(config) {
	 
	var getTestValues = function(fileSystemItem) {
		var result = "bar";
		if (fileSystemItem._id === 0){
			result = "bar1";
		}
		fileSystemItem.icon = result;
	};

	config.app.events.on("foo", getTestValues);

	return this;
};

module.exports = Package;